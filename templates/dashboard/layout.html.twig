<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Musikeo Dashboard{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🎵</text></svg>">
    
    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="bg-gray-100 font-inter">
    <div class="flex h-screen overflow-hidden">
        <!-- Sidebar -->
        <aside class="bg-gray-900 text-white w-64 min-h-screen p-4 fixed lg:static lg:inset-0">
            <div class="flex items-center justify-between mb-6">
                <div class="flex items-center">
                    <div class="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center mr-3">
                        <span class="text-xl">🎵</span>
                    </div>
                    <span class="text-xl font-bold">Musikeo</span>
                </div>
            </div>

            <!-- Navigation Menu -->
            <nav class="space-y-2">
                {% block sidebar_menu %}
                <!-- Menu par défaut - sera remplacé dans les templates enfants -->
                <a href="{{ path('app_home') }}" class="flex items-center px-4 py-2 text-gray-300 hover:bg-gray-800 hover:text-white rounded-lg transition-colors">
                    <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2H5a2 2 0 00-2-2z"></path>
                    </svg>
                    Accueil
                </a>
                {% endblock %}

                <!-- User Info & Logout -->
                <div class="absolute bottom-4 left-4 right-4">
                    <div class="bg-gray-800 rounded-lg p-3 mb-3">
                        <div class="flex items-center">
                            <div class="w-8 h-8 bg-blue-600 rounded-full flex items-center justify-center mr-3">
                                <span class="text-sm font-medium">{{ app.user.firstname|slice(0, 1) }}{{ app.user.lastname|slice(0, 1) }}</span>
                            </div>
                            <div class="flex-1">
                                <p class="text-sm font-medium">{{ app.user.fullName }}</p>
                                <p class="text-xs text-gray-400">
                                    {% if 'ROLE_ADMIN' in app.user.roles %}
                                        Administrateur
                                    {% elseif 'ROLE_TEACHER' in app.user.roles %}
                                        Professeur
                                    {% elseif 'ROLE_STUDENT' in app.user.roles %}
                                        Étudiant
                                    {% endif %}
                                </p>
                            </div>
                        </div>
                    </div>
                    <a href="{{ path('app_logout') }}" class="flex items-center px-4 py-2 text-gray-300 hover:bg-red-600 hover:text-white rounded-lg transition-colors">
                        <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                        </svg>
                        Déconnexion
                    </a>
                </div>
            </nav>
        </aside>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col overflow-hidden lg:ml-0">
            <!-- Top Header -->
            <header class="bg-white shadow-sm border-b border-gray-200 px-4 py-4 sm:px-6 lg:px-8">
                <div class="flex items-center justify-between">
                    <div>
                        <h1 class="text-2xl font-semibold text-gray-900">{% block page_title %}Dashboard{% endblock %}</h1>
                        <p class="text-sm text-gray-600">{% block page_subtitle %}Bienvenue sur votre espace Musikeo{% endblock %}</p>
                    </div>
                    
                    <!-- Actions rapides -->
                    <div class="flex items-center space-x-3">
                        {% block header_actions %}
                        <button class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg flex items-center">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                            </svg>
                            Nouveau
                        </button>
                        {% endblock %}
                    </div>
                </div>
            </header>

            <!-- Page Content -->
            <main class="flex-1 overflow-x-hidden overflow-y-auto bg-gray-50">
                <div class="container mx-auto px-4 py-6 sm:px-6 lg:px-8">
                    {% block body %}{% endblock %}
                </div>
            </main>
        </div>
    </div>

    <!-- Mobile menu button (hidden on desktop) -->
    <button class="lg:hidden fixed top-4 left-4 z-50 bg-gray-900 text-white p-2 rounded-lg">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
    </button>
</body>
</html>